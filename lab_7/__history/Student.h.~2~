//---------------------------------------------------------------------------

#ifndef StudentH
#define StudentH
#include <vcl.h>
class Student
{
public:
	Student();
	Student(AnsiString fullName, AnsiString groupNumber, double averageScore, double income);
	AnsiString fullName;
	AnsiString groupNumber;
	double averageScore;
	double income;
	int priority;
	//const double minimumWage;
	static int addStudent();
	static void viewFile();
	static int getAmount();
};
//---------------------------------------------------------------------------
#endif
